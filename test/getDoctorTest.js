const getDoctor = require('../src/formatServices/getDoctor')
const assert = require('chai').assert

describe('#formatServices', () => {
  it('test if getDoctor function exists', () => {
    assert.isFunction(getDoctor)
  })

  it('test if a list of doctor is called', () => {
    assert.isFunction(getDoctor)
  })
})
